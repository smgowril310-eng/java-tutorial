package Variables;
//if temporary requirement is need then the programmer will give the variable inside the method or constructor are blocks
//we can access only inside the methods or blocks or constructor
//local variable will be created while the method is creating and once the method is finished it execution the local variable automatically destroyed
public class Localvariable {
     public static void main(String[] args) {
    int d=100;
    int e;
    e=200;
    System.out.println(d);
    System.out.println(e);
    }
}